{
  "name": "yume-tsuzuri-orchestrator",
  "version": "1.0.0",
  "description": "Cloudflare Workflow orchestrator for the Yume Tsuzuri PWA",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "wrangler types && tsc --noEmit",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "wrangler": "4.124.0"
  }
}
